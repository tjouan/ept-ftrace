ftrace (ELF tracer, 2004 school project)

Project status (2015):

* build with warnings
* print function calls
* syscall tracing seems broken:
  - hard coded syscall table cannot be correct
  - generation script is not portable
